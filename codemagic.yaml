workflows:
  android-apk:
    name: Android APK
    max_build_duration: 60
    environment:
      java: 17
    scripts:
      - python3 -c "import os, base64; open('debug.keystore', 'wb').write(base64.b64decode(open('debug.keystore.base64', 'r').read().strip())) if os.path.exists('debug.keystore.base64') else print('debug.keystore.base64 not found, skipping restore')"
      - gradle wrapper --gradle-version 9.3.1
      - chmod +x gradlew
      - ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
